import { StyleSheet } from 'react-native';
import { WithTimingConfig } from 'react-native-reanimated';

export const SEGMENTED_CONTROL_ANIMATION_CONFIG: WithTimingConfig = { duration: 300 };

export const SEGMENTED_CONTROL_BORDER_WIDTH = StyleSheet.hairlineWidth * 3;
