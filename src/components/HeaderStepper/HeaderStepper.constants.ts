import { Easing, WithTimingConfig } from 'react-native-reanimated';

export const TIMING_ANIMATION_CONFIGURATION: WithTimingConfig = {
  duration: 1500,
  easing: Easing.in(Easing.ease),
};

export const ANIMATION_DELAY_IN_MS = 100;
